# Proofable Node SDK

## Compile Protobuf Definitions

1. make sure node version is `v12.16.2`. Using `v12.16.3`, you might get [this](https://github.com/grpc/grpc-node/issues/1362) weird `bad download` error

2. `npm install`

3. go to `proofable` project root and `make generate`
